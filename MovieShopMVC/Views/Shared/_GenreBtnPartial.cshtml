@using ApplicationCore.Contracts.Services
@inject IMovieService movieService


<a class="nav-link dropdown-toggle text-dark" asp-area="" asp-controller="Movies" asp-action="Genres" data-bs-toggle="dropdown" aria-expanded="false">
    Genre
</a>
<ul class="dropdown-menu" aria-labelledby="dropdownMenuButton1">
    @foreach (var genre in await movieService.GetGenreList())
    {
        <li>
            <a class="dropdown-item" asp-controller="Movies" asp-action="Genres" asp-route-id="@genre.Id">
                @genre.Name
            </a>
        </li>
    }
</ul>


